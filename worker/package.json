{
  "name": "decepticon-tts-worker",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker that proxies Microsoft Edge neural TTS to the browser.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.95.0"
  }
}
