name: Decepticon · Chronicle

on:
  schedule:
    - cron: "13 9 * * *"
  workflow_dispatch:

permissions:
  contents: write
  models: read

concurrency:
  group: chronicle
  cancel-in-progress: false

jobs:
  write:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Write today's chronicle
        env:
          LLM_MODEL: ${{ vars.LLM_MODEL }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: node scripts/chronicle.mjs

  deploy:
    needs: write
    uses: ./.github/workflows/deploy.yml
    permissions:
      contents: read
      pages: write
      id-token: write
